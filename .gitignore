# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# guest answers plaintext source — never commit real answers
/data/guests.source.json

# claude worktrees
.claude/worktrees

# playwright mcp artifacts (визуальная приёмка: скриншоты, снапшоты, логи консоли)
.playwright-mcp/

# terraform state/vars — contains secrets (storage_secret_access_key)
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.*
terraform/terraform.tfvars
terraform/tfplan*.out
terraform/*.tfplan
